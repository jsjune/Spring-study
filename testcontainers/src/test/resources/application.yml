spring:
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: jdbc:tc:mysql:5.7:///test
  jpa:
    hibernate:
      ddlAuto: create-drop
    properties:
      hibernate:
        show_sql: true
        format_sql: true

